name: Test GitHub Actions Workflow

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run a test command
        run: echo "✅ Hello, GitHub Actions is working perfectly!"
